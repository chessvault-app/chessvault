# The bundled starter reference games land here at release time (see the
# `starter-db` job in .github/workflows/desktop.yml). Empty in a checkout,
# and that is fine: the app seeds a database only if it finds one.
