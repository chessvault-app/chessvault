# 업데이트 로그

*[English](update-log.md) · 한국어*

무엇이 바뀌었는지 최신순으로 적었습니다. 커밋 장부가 아니라 기능 단위의
기록이며, 자세한 내용은 `git log`에 있습니다.

## 미공개

게임 메뉴는 하나로, 참고 게임 층은 수백만 판 규모로, 데이터베이스는 다시
만드는 대신 자라는 것으로.

- **모든 게임 목록 아래 하나의 셸.** 모음, 온라인 아카이브, 참고 게임
  브라우저는 같은 띠들 — 툴바, 필터 행, 개수 띠, 목록, 빈 상태 — 을 각자
  손으로 조립했고, 경계선 방향, 개수 띠 높이, 스켈레톤 위치, 폰 시트에서
  서로 어긋나 있었습니다. 이제 `GameListShell`이 그 더미를 경계선 규칙
  하나와 모양 어휘 하나(framed / panel / sheet)로 소유합니다. 참고 게임의
  행은 공유 `GameRow`가 되어(긴 이름 뒤에서 레이팅이 더는 잘리지 않고,
  터치 미리보기는 어디서나 ⋯ 시트에 삽니다), 모든 목록이 줄무늬를 입고,
  시트는 카드의 진짜 가장자리까지 번지고, 두 시트 모두 보드에 다녀와도
  살아남고, 검색이 빈손으로 돌아오면 이제 빠져나갈 길과 함께 그렇게
  말합니다. 아무도 가지 않던 `#/games/elite` 페이지는 사라졌습니다.
- **"엘리트 게임"은 제 이름을 찾았습니다: 데이터베이스.** 브라우저가
  보여 주는 것은 엘리트든 아니든 직접 만든 데이터베이스들이고, 탭과 게임
  추가 항목과 시트가 이제 그렇게 — 관리하는 페이지와 같은 이름으로 —
  말합니다.
- **데이터베이스 검색이 규모를 견딥니다.** 선수와 오프닝 텍스트 검색은
  모든 행의 텍스트 대신 작은 파생 조회 테이블 위를 달리고(글자 하나마다
  코퍼스와 함께 자라는 스캔을 치르던 것이), 페이지는 OFFSET 대신 키셋으로
  이어 받으며(깊이 22.4만에서 70 ms → 0.1 ms, 측정), 걸러진 개수는 이백만
  행을 끝까지 세는 대신 "10,000+"에서 멈춥니다.
- **데이터베이스가 제자리에서 자랍니다.** 이미 있는 이름으로 만들면
  "추가"를 제안합니다: 아직 없는 게임만 색인되고(선수·결과·날짜·기보로
  찾아 들어가는 중복 제거), 포지션 색인은 다시 만드는 대신 고수위선에서
  이어집니다. 끊긴 추가는 데이터베이스를 서빙하면서 "색인이 뒤처짐"으로
  표시해 두고, 행마다 새로 생긴 최적화가 그것을 고치며 — 완전히 같은
  게임을 쓸어 내고, 전부 다시 유도하고, 파일을 압축합니다.
- **탐색기가 내 수준에서 답합니다.** 수별 합계가 200점 레이팅 구간 차원을
  갖게 되어, 참고 필터의 "레벨" 구간(1200–1599 … 2400+)이 미리 계산된
  행을 읽습니다 — 코퍼스에서 가장 강한 사람들이 아니라 내 기력의
  선수들에게서 온 통계입니다. 색인 작업 자체도 Elite 한 달치에서 105초에서
  78초로 줄었고(합계가 더는 조인하지 않습니다), 얇은 포지션은 실시간으로
  답해 행을 92% 덜 저장합니다.
- **어떤 포지션이든, 모든 게임을 뒤져서.** 탐색기가 바닥나면 — 색인은
  30수(플라이)에서 멈춥니다 — 현재 포지션을 모든 게임의 기보에서 찾아
  주겠다고 제안합니다. 진행 상황과 찾은 게임이 스트리밍으로 도착합니다
  (28만 판을 필터 없이 9–13초, 필터를 걸면 수 초, 측정).
- **내 게임을 데이터베이스에 비추어.** `/api/mygames/compare`가 참고
  코퍼스의 선수들이 두는 수 가운데 내 수가 드문 포지션들을 — 레벨 구간을
  주면 내 수준에서 — 최근 게임 전체에 걸쳐 집계해, 가장 잦은 습관부터
  보여 줍니다. 큰 보관함의 첫 동기화가 그것을 시작한 요청을 몇 초씩
  세워 두던 것도 함께 사라졌습니다. 이 리포트는 오프닝 맵이 담습니다:
  **내 수를 데이터베이스와 비교**가 코퍼스와 레벨을 고르고, 가장 잦은
  습관부터 읽어 주며, 표시된 어느 줄이든 결정 지점에서 보드에 엽니다.
- **무거운 작업들의 네이티브 빠른 길, 구조적으로 선택 사항.**
  `native/`에는 빌드·색인·최적화·전수 검색 작업을 바이트 단위로
  그대로 옮긴 Rust 바이너리 `chessvault-core`가 있습니다 — JS
  파이프라인 자신의 고정 답안으로 골든 테스트를 거치고, Elite 한
  달치를 다시 빌드해 다른 행이 0개임을 증명했습니다. 빌드된
  바이너리가 있으면 서버가 JS 자식 대신 그것을 띄웁니다(같은 인자
  형태, 같은 진행 줄, 같은 ndjson 프레임; `CHESS_NATIVE=0`은 JS
  경로를 강제합니다): 28만 판에서 측정하면 전체 빌드가 약 180초에서
  71.8초로, 데이터베이스 전체 포지션 검색이 12.7초에서 1.3초로,
  메모리는 4분의 1로 줄어듭니다. 그 속도에서는 탐색기가 묻지
  않습니다: 색인의 30수를 지나면 스스로 검색을 시작하되, 게임을
  화살표로 넘길 때 수마다 검색이 시작되지 않도록 잠깐 기다리고,
  포지션이 바뀌는 순간 취소합니다 — 버튼은 더 느린 JS 검색의 얼굴로
  남습니다. 바이너리가 없으면 — 새로 받은 저장소, 데모, 테스트 —
  모든 것이 이전 그대로 동작합니다. 아직 릴리스에 실리지는 않습니다;
  그것(그리고 크레이트들의 라이선스 목록 정리)이 앞에 남은 배포
  단계입니다.

## 0.4.9

책에는 우드페커 방법론을, 전술에는 복습 일정을, 모든 컨트롤 밑에는
Base UI를.

- **퍼즐 책의 우드페커 사이클.** 전술 책은 유한하고 선별된 문제집이고,
  그 책이 쓰인 방법론은 그 문제집을 조금씩 흘려 주는 게 아니라 통째로
  돌리는 것입니다. 책 페이지에 사이클 패널이 생깁니다: 패스를 시작하고,
  아직 닿지 않은 첫 퍼즐로 이어 가고, 멈추고, 끝난 패스들을 성공/실패
  막대와 함께 행으로 읽습니다. 패스가 열려 있는 동안 격자 전체 — 타일,
  막대, 필터 개수 — 가 그 패스의 창을 통해 읽히므로, 사이클을 시작하면
  판이 눈에 보이게 비워지고 멈추면 전체 기록이 돌아옵니다. 트레이너의
  완료 행은 단 하나의 다음 길, "사이클의 다음"만 내밀고, 서가 카드는 각
  책의 회전이 어디까지 왔는지 말합니다("사이클 2 — 412/959"). 패스는
  마지막 퍼즐을 채우는 그 시도에 스스로 닫히고, 아무도 풀지 않은 패스는
  없었던 일이 됩니다. 저장되는 것은 패스의 창뿐입니다: 시도, 성공, 다음
  퍼즐이 전부 진행 기록에서 유도되므로, 기록과 어긋날 점수라는 것이
  존재하지 않습니다.
- **전술이 복습 일정을 배웁니다.** 두 트레이너 모두 무엇을 틀렸는지는
  기억했지만 *언제* 다시 물을지는 몰랐습니다 — 틀린 퍼즐은 영원히 복습
  대상이었고, 한 번 겨우 맞힌 퍼즐은 영영 끝난 것이었습니다. 이제 공유
  사다리(1, 3, 7, 21일, 마지막 실패 이후의 깔끔한 성공 횟수 기준)가 모든
  퍼즐의 일정을 잡습니다: 복습 모드는 기한이 온 퍼즐을, 가장 많이 밀린
  것부터 먼저 내고, 책의 기한 도래 수는 서가 카드에 앉으며 책 트레이너는
  "다음 복습"으로 대기열을 이어 갑니다. 대시보드와 허브는 기한이 온 것을
  앞세우고, 아무것도 없을 때는 다음 복습이 언제 오는지 말합니다 — 빈
  대기열이 사라진 게 아니라 벌어 낸 것으로 읽히도록. SM-2가 아니라 고정
  사다리인 것은 의도입니다: 전술은 스스로 채점되고, 난이도 버튼은 푼
  사람에게 스스로 매길 판정을 쥐여 주는 일이기 때문입니다.
- **희생은 상대의 응수가 아니라 포지션에서 읽습니다.** 게임 리뷰는
  수 이후 두 플라이의 재료 셈으로 희생을 표시했는데, 그것은 "상대가
  무언가 잡았는가"라는 엉뚱한 질문의 답입니다: 조용한 수 뒤에 상대가
  지켜진 기물을 덥석 잡으면 두는 쪽이 감탄부호를 받았고, 상대가 거절한
  진짜 희생 제안은 표시되지 않았습니다. 이제 희생은 그 수가 만든
  포지션을 재료만 보는 정지 탐색으로 가라앉혀 판정합니다.
  카스파로프–토팔로프 1999에서 표시되는 것은 정확히 그 불멸의
  콤비네이션입니다.
- **탐색기의 플레이어 데이터베이스가 레이팅 밴드를 받습니다.** 창이
  이미 갖고 있던 필터 창이 플레이어 소스에도 열리고, 커버리지
  다이얼로그가 내밀던 밴드 칩이 그대로 옵니다. 선택은 다른 필터처럼
  유지됩니다. 마스터즈에는 창이 없습니다: 그 모집단은 그룹이 아니라
  자격이기 때문입니다.
- **편집기가 제 방향을 지킵니다.** 분석은 이제 포지션과 함께 판의
  뒤집힘도 넘기고, 브라우저의 뒤로는 떠날 때의 포지션, 도구, 방향으로
  돌려 놓으며, 편집기를 새로 여는 길 — 탭, 더보기 타일, 북마크 — 은
  깨끗하게 시작합니다. 온라인 아카이브 창도 같은 것을 배웠습니다:
  기보를 열면 체스판으로 이동하는데, 돌아오면 이제 창이 열린 채로
  맞이합니다.
- **마지막 수제 컨트롤들이 레지스트리를 조립합니다.** 네이티브
  체크박스, 라디오, 레인지 입력, 날 것의 `<hr>`, 손으로 돌리던 스피너,
  손으로 붙이던 레이블, 손으로 그리던 빈 상태가 레지스트리의 Checkbox,
  RadioGroup, Slider, Separator, Spinner, Field, Empty가 되었습니다.
  패널은 레지스트리 자체의 간격 모델을 호출부에서 되찾아 오고, 버튼 옆
  아이콘은 레지스트리의 방식으로 자신을 선언합니다. 나이트 판화는
  은퇴합니다 — 빈 서가는 마스코트 대신 제 섹션의 아이콘을 답니다.
- 자잘한 것들: 오프닝 맵의 배치 전환이 더는 드래그가 남긴 자리에 점을
  다시 그리지 않고, 온라인 메인라인 추적 중 페이지를 얼릴 수 있던 캐시
  키 불일치가 고쳐졌습니다. 이탈 기보 목록은 표답게 줄을 맞추고,
  무승부는 탐색기와 같은 칩 위의 ½ 한 글자입니다. 거절된 업로드가 더는
  `.part` 조각을 남기지 않고, 보관함의 모든 이름 바꾸기가 업로드 하나를
  잃게 했던 윈도우의 일시적 오류를 재시도합니다.
- **컴포넌트 층이 Base UI 위에 섭니다.** shadcn/ui 컴포넌트 밑의 모든
  프리미티브 — 다이얼로그, 메뉴, 셀렉트, 툴팁, 탭, 토글, 스위치, 슬라이더
  등 — 가 이제 `@base-ui/react`입니다. Radix를 만들던 팀이 옮겨 가 만드는
  라이브러리이고, `radix-ui`는 트리에서 사라졌습니다. 기기에서 배운 것은
  하나도 버리지 않았습니다. 휴대폰에서 모든 창은 여전히 아래쪽 시트이고,
  뒤로 갈매기표, 키보드 띠, 입력란 하나짜리 창의 포커스, CloseWatcher를
  통한 안드로이드 뒤로가 모두 그대로이며, 예전 빌드와 견준 48장 스크린샷
  격자에서 42장이 바이트 단위로 동일했고 나머지는 설명되는 차이(안티앨리어싱
  노이즈, 그리고 매번 무작위 퍼즐을 그리는 퍼즐 페이지)뿐이었습니다.
- **시트의 스와이프 물리가 Base UI의 Drawer가 되었습니다.** 휴대폰에서
  모든 창이 `Dialog` 대신 `Drawer` 위에서 움직이고, 앱이 직접 쓰던 235줄
  드래그 훅은 은퇴했습니다. 아무 데나 잡고 끄는 제스처, "손가락 아래
  스크롤러가 맨 위에 닿기 전까지 드래그는 스크롤"이라는 중재, 안드로이드
  뒤로가 모두 프리미티브에서 옵니다. 앱이 계속 소유하는 것은 프리미티브에
  없는 것들입니다: 페이지/층 주차와 뒤로 갈매기표, 키보드 띠, 입력란
  하나짜리 창의 포커스, 글자 입력란이나 캔버스 위의 터치는 드래그가 되지
  않는다는 규칙, 그리고 휴대폰에서는 열리고 닫힐 때 아무것도 움직이지
  않는다는 규칙(스와이프의 180ms 복귀만 애니메이션). 신뢰된 실제 입력으로
  증명했습니다: 스와이프가 시트를 움직이고, 짧으면 제자리로, 길면 닫히며,
  스크림 탭과 Esc도 그대로 닫습니다.

## 0.4.8

체스 책 서재 — 체스판 옆에서 읽습니다.

- **책.** 새 최상위 메뉴입니다. 어떤 체스 책이든 — 전략서, 기보 모음집,
  오프닝 책, 퍼즐 책 — PDF로 올리면 앱 안에서 읽을 수 있습니다. 크기를
  조절할 수 있는 창에 책을, 그 옆에 분석 보드와 수순 패널과 포지션 불러오기를
  둡니다. 휴대폰에서는 책·보드 탭으로 나뉘고, 아래 막대가 한쪽에서는 쪽을
  넘기고 다른 쪽에서는 수를 옮깁니다. 파일은 보관함(`vault/books/<id>/`,
  히스토리 저장소에는 절대 들어가지 않음)에 있고 바이트 범위로 서빙되므로,
  한 쪽은 그 쪽의 바이트만 들고 휴대폰 메모리에 책 전체를 올리는 일이
  없습니다. 읽던 쪽은 책마다 서버에 기억되어 기기를 옮겨도 따라옵니다.
  카드는 파일 크기와 멈춘 쪽을 보여 주고, 이름 바꾸기·파일 바꾸기·제거는
  카드의 메뉴와 읽기 화면의 머리글에 있습니다.
- **인쇄된 다이어그램은 모두 한 번의 탭으로 판에 놓입니다.** 읽기 화면은
  쪽의 다이어그램마다 작은 체스판 버튼을 그립니다. 같은 PDF에서 읽어 낸
  퍼즐 책이 있으면 그 퍼즐의 포지션과 차례가 바로 판에 놓이고, 그 밖의 책은
  책이 들어올 때(배경 작업으로, 또는 카드의 “다이어그램 읽기”로) 가져오기의
  검출기와 CellNet 워커 풀로 쪽마다 한 번 읽어 서버에 저장해 둡니다. 이
  작업은 필수이고 이어 갈 수 있습니다: 다 읽기 전에 책을 열면 작업이
  시작되거나 이미 읽은 쪽 다음부터 이어지며 진행 상황이 쪽 위에 표시되고,
  함께 요청된 책은 차례로 읽습니다. PDF에서 가져온 퍼즐 책은 두 번 읽지
  않습니다 — 가져오기의 스캔이 쪽마다 다이어그램을 서재 사본에 그대로
  적어 둡니다. 322쪽짜리
  퍼즐 책을 12코어 기계에서 3분쯤에 읽는데, 첫 판은 메인 스레드에서 판을
  하나씩 읽어 한 쪽에 5초가 걸렸습니다. 버튼은 추측하는 대신 백 차례인지 흑 차례인지 묻습니다 — 잘못 읽은
  다이어그램이라면 편집기에서 포지션을 고쳐 올릴 수 있고, 체스판 머리글에도
  지금 판 위의 포지션을 고치는 같은 길이 있습니다. 책은 쪽이 이어진 한 열로
  스크롤되며 보이는 쪽만 그려지고, 읽기 화면은 토글 하나로 쪽을 창 너비에
  맞추거나 쪽 전체를 보여 주고, 번호로 쪽을 찾아갑니다. 휴대폰의 핀치는
  손가락을 따라 움직이다 손을 떼면 한 번만 다시 그리고, 확대·축소는 쪽
  맨 위로 튀는 대신 손가락(또는 창 가운데) 아래의 지점을 그 자리에 둡니다.
  다이어그램 버튼이 있는 자리에 설명을 인쇄한 책을 위해 도구 막대에서
  버튼을 숨길 수 있습니다. PDF 옆에서 수순
  패널은 자리가 있으면 체스판 옆에, 없으면 아래에 놓이고, 휴대폰에서는
  읽기 화면이 체스판 페이지와 같은 모양입니다 — 위에 체스판, 아래에 책·수순·
  엔진 패널.
- **책을 모음에 정리합니다.** 스터디·노트처럼: 서재에 모음마다 제목이
  붙고 서재 자체의 책이 먼저 옵니다. 만들기 아래의 “새 모음”, 카드의
  “모음으로 옮기기”, 올리기 창의 대상 모음, 그리고 제목 메뉴에서 이름을
  바꾸거나 (비었을 때) 삭제합니다. 모음은 디스크의 폴더가 아니라 책에
  적힌 이름이라, 책의 id도 — 그것을 가리키는 퍼즐 책도 — 움직이지
  않습니다.
- **가져오기가 스캔한 PDF를 서재에 보관합니다.** 퍼즐 책을 가져오면 PDF도
  서재에 들어가고 퍼즐 책이 그것을 가리키므로 책 페이지에 읽기가 생깁니다.
  서재의 책을 제거해도 퍼즐 책과 진행 기록은 그대로입니다. 서재가 “책”이 된
  만큼, 퍼즐 아래의 서가는 어디서나 “퍼즐 책”이라고 부릅니다.
- **설정에 영역별 저장 공간 사용량이 생겼습니다.** 게임, 스터디, 노트, 책,
  퍼즐 책, 퍼즐 진행 기록, 레퍼토리, PGN 원본, 둘러본 게임 캐시, 문서 기록,
  참고 데이터베이스, 탐색기·테이블베이스 캐시와 합계입니다 — 목록일 뿐이고,
  비우는 것은 각 영역의 페이지에서 합니다.

- **자잘한 것들.** 두 서가의 섬네일이 첫 로드만이 아니라 언제나 한꺼번에
  나타납니다. 책을 열 때 로딩 표시는 한 가지로, 첫 쪽이 실제로 그려질
  때까지 유지됩니다. 읽기 화면의 도구 막대는 칸이 좁으면 보기 조절을
  “…” 메뉴로 접습니다. 색 구성 드롭다운이 구성마다 점을 보여 줍니다.
  게이트의 인증 코드는 여섯 칸 입력입니다. 곁줄에서 한 수 이동해도 수순
  패널이 맨 위로 스크롤되지 않습니다.

## 0.4.7

앱이 shadcn/ui 프로젝트가 되고, 가져오기는 더는 지켜보지 않아도 됩니다.

- **앱이 shadcn/ui 프로젝트가 되었습니다.** 모든 컨트롤이 레지스트리의
  컴포넌트입니다 — Button, Input, Dialog, DropdownMenu, Select, Tooltip,
  Tabs, Switch 등, 밑은 Radix — `web/src/components/ui`에 가져와 이 앱의
  얼굴을 입혔고, 기기에서 배운 것은 하나도 버리지 않았습니다. 휴대폰에서
  모든 창은 여전히 아무 데나 잡고 끌면 밀려나는 아래쪽 시트이고, 뒤로
  갈매기표는 여전히 창의 페이지를 넘기며, 키보드 띠도 그대로이고,
  안드로이드의 뒤로는 여전히 맨 위 층을 닫습니다. 키보드와 화면 읽기
  프로그램 쪽은 더 나아졌습니다. Esc 한 번은 맨 위 층 하나를 닫고, 열리면서
  입력 칸이 포커스를 가져간 창을 닫아도 포커스는 그 창을 연 컨트롤로
  돌아가며, 도구 설명은 키보드 포커스에도 뜨고, 메뉴는 글자로 찾고,
  분할 선택은 radiogroup, 패널 전환은 진짜 tablist입니다. 테마는 앱의
  OKLCH 사다리에서 파생한 shadcn의 토큰 어휘로 적혀 있어 색조·채도·대비
  손잡이는 그대로 동작하고, 내일 레지스트리에서 가져오는 컴포넌트도 놓이는
  즉시 테마를 입습니다. 이행 자체는 두 너비·두 테마의 44장 격자
  스크린샷으로 재어 모든 단계에서 픽셀 단위로 같았고, 그다음에 겉모습을
  놓아주었습니다. 이제 앱은 shadcn 자신의 디자인을 입습니다 — 뉴트럴
  테마(새 기본 배색이고, 예전 모습인 Slate는 설정에서 한 번 누르면
  돌아옵니다), 크기와 모서리 사다리, 포커스 링, 대화상자·메뉴·도구
  설명·카드까지. 설정 → 모양에는 shadcn 자신의 손잡이가 들어왔습니다. 앱의
  색조 배색 옆에 shadcn의 기본 색 다섯 가지(뉴트럴, 스톤, 징크, 그레이,
  슬레이트)가 배색으로, 그리고 앱의 모든 모서리가 따르는 모서리 반지름
  설정(각진, 작게, 기본, 크게)이 생겼습니다. 날짜 필터는 레지스트리의 날짜
  선택기(팝오버 속 달력, 앱의 언어로)이고, 모든 확인 질문은 레지스트리의
  파괴적 동작용 alert dialog입니다. 잘라낸 화면을 재 본 뒤 레지스트리의
  숫자 둘을 다시 맞췄습니다. 눌림·선택 채움은 호버 채움보다 한 단 위에
  놓이고(카드 위에서 눌린 토글이 주변과 밝기 3%(라이트)·6%(다크)밖에 차이
  나지 않아 보이지 않았습니다), 다크의 입력 테두리 색은 shadcn 자신의
  반투명 흰색입니다 — 선택된 탭의 알약이 거기에 기댑니다. 탭, 토글,
  토글 그룹 항목도 버튼처럼 `title`을 툴팁으로 보여 줍니다.

- **스크린샷과 랜딩 페이지가 새 디자인을 입었습니다.** README와 사이트의
  모든 이미지를 데모에서 두 테마로 다시 캡처했고, `<picture>`가 읽는 사람의
  시스템 설정에 맞는 쪽을 고릅니다 — 앱이 그 설정을 따르므로, 어두운
  페이지 위의 흰 스크린샷은 클릭하면 열리는 앱과 다른 앱의 그림이었습니다.
  랜딩 페이지는 shadcn의 뉴트럴 테마를 손으로 옮겨 적어 다시 썼습니다(같은
  토큰, 헤어라인 링 카드, `--radius` 사다리, outline·primary 버튼, Tabs
  모양의 언어 전환). 기본은 밝은 테마이고 `prefers-color-scheme`에 따라
  어두워지며, 전에는 어두운 테마뿐이었습니다. 캡처 스크립트는 테마를 앱
  자체의 스토어로 강제하고, 실행하는 사람의 기기에서 물려받지 않습니다 —
  지난 세트가 어둡게 나온 이유가 그것이었습니다.

- **이전 뒤의 다듬기, 휴대폰에서 재면서.** 게임 패널의 출처 탭은 shadcn의
  밑줄 Tabs 그대로입니다(꾸미던 클래스가 한 번도 적용된 적이 없어,
  레지스트리의 도드라진 알약이 밑줄 띠 안에 앉아 있었습니다). 스터디
  카드는 보드를 위에 붙이지 않고 제목과 가운데를 맞춥니다. 모든 패널
  헤더는 높이가 하나 — 44px, 터치 화면에서는 52px — 라서 휴대폰의 창
  탭이 수순·엔진·챕터·탐색기 사이를 오갈 때 제목이 더는 뛰지 않습니다.
  휴대폰의 동작 시트는 줄들을 제목 아래에 맞춰 세웁니다. 패널들은 동작을
  shadcn의 CardFooter 위에 둡니다 — 퍼즐·책 트레이너, 레퍼토리, 그리고
  네 버튼이 패널과 탭 바 사이에 떠 있던 퍼즐 정답 기록기(헤더의 되돌리기는
  빠졌습니다 — 처음부터가 그 일을 합니다). 설정 → 모양의 색 배색은
  카드의 나머지처럼 드롭다운이고, shadcn / 색상 / 대비로 묶여 있습니다.
  수순 목록에서 사이드라인의 수는 lichess처럼 첫 수에서 이어 흐르고(첫
  수가 제 줄에 홀로 서 있었습니다), 사이드라인은 모든 깊이에서 직각
  가지선에 매달립니다 — 형제가 더 따르면 ├, 마지막은 └.

- **'실패한 보드에 더 공을 들이기'도 기계 전체를 씁니다.** 이름이 가리키는
  탐색은 원래 비용이 아니었습니다. 실패한 보드 하나에서 후보 포지션을 다시
  놓아보는 데 33밀리초, 그 보드를 다시 읽는 데 몇 초입니다 — 평소와 같은
  분류기를, 조금 밀어서 읽었을 때 뒤집히는 칸을 잡아내려고 다섯 번 돌리기
  때문입니다. 그 다시 읽기는 이미 풀 위에서 돌 수 있었는데 쓰지 않았습니다.
  보드를 하나씩 내보내서, 여섯 중 다섯이 노는 동안 하나만 일했습니다. 이제는
  잘라내는 대로 내보내고 읽힌 순서대로 탐색합니다. 바로 옆의 스캔이 늘 하던
  방식입니다. 보드도, 후보도, 거절하는 기준도 그대로입니다 — 같은 퍼즐이 같은
  순서로, 더 일찍 돌아옵니다.

- **보드 하나를 읽는 속도가 네 배가 되었고, 모든 가져오기가 그 덕을 봅니다.**
  보드 읽기의 절반은 3x3 합성곱 하나인데, 이미지 밖으로 벗어날 수 있는 것은
  가장자리 한 픽셀뿐인데도 모든 곱셈마다 그것을 확인하고 있었습니다. 가장자리와
  가운데를 나누니 1.73배입니다. 답은 비트 단위로 같습니다 — 예전 코드와 확률
  832개를 맞춰 보았고 다른 것은 없었습니다. 나머지 절반은 수리용 읽기가 64칸
  전부를 네 번씩 더 보는 부분입니다. 보드가 밀렸을 때 생각을 바꾸는 칸을
  잡으려는 것인데, 분류기가 확신하는 칸은 그러지 않습니다. 실제 책의 2,496칸
  가운데 뒤집힌 93칸은 모두 여유가 0.4 미만이었습니다. 이제 아슬아슬한 칸만
  다시 읽습니다. 안전을 위해 그 두 배를 기준으로 삼았고, 보드당 320번이던
  읽기가 153번이 됩니다.

- **가져오기를 누가 지켜보고 있지 않아도 됩니다.** 창을 가려 두면 스캔이 1쪽에서
  멈춘 채 아무 말도 하지 않았습니다. pdf.js는 한 쪽을 여러 조각으로 나눠 애니메이션
  프레임에 맞춰 그리는데, 아무도 보고 있지 않는 창에는 그 프레임이 오지 않기
  때문입니다 — 재어 보니 2초 동안 0개였습니다. 이제는 그렇게 예약하지 않는 유일한
  방식으로 그립니다. 그려지는 그림은 달라지지 않습니다. 책이 될 수 있는 두 종류의
  쪽, 글자와 사진 모두 바이트 단위로 같았습니다. 쪽과 쪽 사이의 기다림도 타이머를
  쓰고 있었는데, 브라우저는 배경 탭의 타이머를 일부러 늦춥니다. 이제는 쓰지
  않습니다. 긴 책을 걸어 두고 다른 일을 하세요.

- **검정과 하양이 실제로 들어 있는 흑백 배색.** 흑백은 앱에서 색을 빼면서
  밝기는 그대로 두었습니다. 그래서 이름이 약속한 두 가지가 실제로는 없었습니다.
  어두운 테마의 페이지는 밝기 15.5%에서 멈추고 가장 밝은 패널이 29%까지밖에
  가지 않았으니, 앱 전체가 밝기 눈금의 가운데에 모여 있고 양 끝에는 아무것도
  없었던 셈입니다. 고대비는 그 배색에서 눈금을 끝까지 연 것입니다 — 어두운
  테마에서는 #000000 페이지 위에 #ffffff 버튼이, 밝은 테마에서는 그 반대가
  놓이고, 글자는 페이지 대비 21:1입니다. 따로 만든 팔레트가 아니라 기존 세
  숫자짜리 배색 모델에 네 번째 숫자를 더한 것이라, 먼저 있던 배색 여덟 개는
  건드리지 않았습니다. 토큰 값 320개를, 모든 프리셋을 두 테마에서 재어 앞뒤가
  픽셀 단위로 같았습니다. 흑백도 그중 하나이며 지금까지의 부드러운 회색
  그대로입니다.

## 0.4.6

책, 그리고 내가 한 일의 기록에 관한 릴리스입니다. 스캔한 책을 기계가 실제로
낼 수 있는 속도로 앱에 읽어들이는 것, 그리고 바꾸거나 지운 문서를 도로
찾아오는 것. 서른아홉 개의 변경입니다.

- **스캔한 책을 읽을 때 기계 전체를 씁니다.** 보드 하나를 분류하는 데 거의
  1초가 걸리고 그게 전부인데, 그것을 작업자 하나가 처리하는 동안 나머지
  코어는 놀았습니다 — 보드 천 개짜리 책이면 20분입니다. 보드끼리는 서로
  독립이므로 이제 코어 하나만 남기고 기계만큼 넓은 풀로 내보냅니다. 그
  하나가 남아 있어서 도는 동안에도 앱을 계속 쓸 수 있습니다. 퍼즐 1,001개
  책의 다이어그램 1,033개를 12코어 기계에서 314초에 읽습니다.

- **책의 정답이 답하지 못한 보드는 엔진이 답합니다.** 인쇄된 정답이 엉망으로
  스캔되면 포지션만 남고 거기 붙는 것이 없었습니다. 이제 그런 보드는 대신
  탐색해서, 얼마나 알아냈는지에 따라 표시를 달아 가져옵니다 — 엔진의 수가
  책이 인쇄한 칸에 떨어지면 가장 높고, 결정적이지만 대조할 것이 없으면 그
  아래, 포지션이 합법이고 둘 차례만 알면 다시 그 아래입니다. 믿고 넘어가는
  것은 없습니다. 모든 수는 저장될 포지션에서 다시 재생해 보고, 재생되지
  않는 것은 초안으로 남습니다. 1,001 책에서는 읽히지 않던 보드 276개가
  퍼즐 269개가 되었습니다.

- **그 엔진 단계는 137초가 아니라 8초입니다.** 포지션을 하나씩, 0.5초로
  고정된 시계에 맞춰 탐색했기 때문에 가져오기에서 유일하게 기계가 빨라도
  빨리 끝나지 않는 단계였습니다. 이제는 단일 스레드 엔진들의 풀이 시계를 다
  쓰는 대신 정해진 깊이에서 멈춥니다 — 같은 책 같은 기계에서 16.7배이며
  처음부터 끝까지 실측했습니다. 대가는 그것이 생기는 자리에 적었습니다.
  예전에 단언하던 보드 열세 개가 이제 한 등급 아래로 표시되어 들어옵니다.

- **모든 퍼즐이 자기 정답이 실린 페이지를 지니고 옵니다.** 초안뿐 아니라
  검증된 등급도 그렇습니다. 인쇄된 정답이 있는 바로 그 등급이 정작 그것을
  대조해 볼 수 없던 상태가 끝났습니다.

- **책의 퍼즐이 책의 순서대로 돌아옵니다.** 가져오기는 패스를 나눠 쓰기
  때문에, 갓 가져온 책은 955, 956 … 1001을 지나서야 2, 4, 10으로 갔습니다 —
  번호가 아니라 얼마나 확실하게 읽혔는지로 묶인 순서였습니다. 다음·이전,
  그리드, "다음 미해결"이 모두 그것을 물려받았습니다. 내보낼 때 인쇄된
  번호로 정렬하면 이미 가져온 책들도 파일을 다시 쓰지 않고 고쳐집니다.

- **보관함의 기록을 앱 안에서 읽을 수 있습니다.** 첫 릴리스부터 모든 문서의
  모든 변경이 `vault/.history.git`에 자동 커밋되어 왔지만, 그것을 쓰려면
  터미널이 필요했습니다. 이제 스터디·게임·노트 머리글의 시계 아이콘이
  저장된 시점들을 보여 주고, 그때의 내용을 확인한 뒤 되돌릴 수 있으며, 아예
  삭제된 문서는 설정 → 삭제된 문서에서 되살립니다. 복원은 파일을 제자리에
  덮어쓰지만, 덮이는 상태를 먼저 커밋하므로 잠시 뒤 같은 목록에서 다시
  되돌아올 수 있습니다. 기록을 남기지 않는 보관함(git이 없거나 데모)에서는
  두 화면 모두 오류 대신 그 사실을 그대로 알려 줍니다.

## 0.4.5

기하에 관한 릴리스입니다. 낮은 화면, 옆으로 누운 화면, 세워 든 화면 —
그림이 그려진 그 화면이 아닐 때 앱이 무엇을 하는가. 서른여섯 개의 변경이며
거의 전부 픽셀 단위로 실측했습니다.

- **세워 든 iPad Pro도 다른 모든 세로 화면처럼 쌓입니다.** 세로는 쌓는다는
  것이 줄곧 규칙이었습니다. 높은 화면은 커다란 보드와 그 아래의 패널을
  원하니까요. 그런데 그 규칙이 이유로 이름을 대던 바로 그 기기가 규칙에서
  빠져 있었습니다. 너비만 보는 가지가 1024px 이상이면 좌우로 나누게 했고,
  1024는 정확히 iPad Pro 12.9인치의 세로 너비입니다. 1024x1366에서 실측한
  보드는 448px, 화면의 40%였습니다. 1픽셀 좁은 곳, 이미 세로 규칙이 걸리던
  자리에서는 792px에 98%였고요. 이제 넓은 쪽이 됩니다. 13인치 M4는 448에서
  800이 됩니다. 대가는 분명히 적어 둡니다. 1024 이상으로 세워 둔 데스크톱
  창도 함께 쌓입니다.

- **낮거나 누운 창은 잘라내는 대신 스크롤합니다.** 낮은 가로 화면에서 보드가
  아무것도 아닌 크기로 무너지지 않도록 최소 높이를 걸어 두었는데, 그 높이가
  넘친 만큼은 그냥 잘려 나갔고 그 사실을 알릴 스크롤바가 어디에도 없었습니다.
  740x360에서 실측하니 보드 페이지가 61px, 편집기가 71px, 레퍼토리가 59px,
  두 트레이너가 45px씩 잃고 있었습니다. 다섯 페이지, 이제 아무것도 잃지
  않습니다. 옆으로 든 휴대폰에서는 옆 칸이 칸이라고 할 수 없을 만큼 눌리기도
  했습니다. 667x375에서 40px짜리 내용 둘레의 기보 패널이 18px이었습니다.
  이제 칸이 최소 높이를 지키고, 대신 페이지가 양보합니다.

- **패널은 자기가 말하는 만큼의 높이입니다.** 휴대폰에서 칸이 페이지 배경의
  띠로 끝나지 않도록 패널을 하단 바까지 늘려 두었는데, 그렇게 사들인 것은 세
  줄짜리 글 둘레에 화면 높이만큼 그려진 패널이었습니다. 그 절반이 테두리
  안쪽의 빈자리였고요. 글 아래의 빈 곳은 패널이 아니라 페이지의 것입니다.
  두 트레이너의 퍼즐 패널과 레퍼토리의 게임 패널이 다시 내용의 높이를 갖고
  거기서부터 줄어듭니다. 칸의 바닥에 닿으면 테두리가 가장자리를 넘어가는
  대신 본문이 스크롤합니다. 이제 모든 보드 페이지에서 마지막 패널 아래의
  띠가 똑같이 12px입니다. 어떤 곳은 32였고 어떤 곳은 12였습니다.

- **버튼은 자기가 답하는 글과 함께 스크롤합니다.** 버튼 행을 패널 바닥에
  고정해 두면 엄지가 늘 같은 자리에서 찾을 수 있지만 두 가지 대가가 있습니다.
  뒤로 글이 흐르는 동안 행만 제자리에 서 있어 패널 하나가 둘처럼 읽히고,
  바닥은 줄어들 수 없으므로 낮은 창에서는 아무도 손을 뻗지 않는 행을 화면에
  두려고 글을 두어 줄로 눌러 버립니다. 375x600에서 책 트레이너의 본문은
  필요한 92px 중 51px이던 것이 필요한 152px 중 111px이 됩니다.

- **모든 버튼 행이 이 앱의 모든 창이 끝나는 방식으로 끝납니다.** 오른쪽 정렬,
  8px 간격, 주 버튼이 마지막. 책 트레이너의 주 버튼은 행의 앞에 서서 폭을
  가로질렀고, 퍼즐 트레이너의 정렬은 퍼즐이 끝나는 순간 한쪽 끝에서 반대쪽
  끝으로 튀었으며, 그 자리가 나온 기보로 나가는 링크는 본문 속 파란 글씨로
  앉아 있었습니다. 이제 두 트레이너 모두 한 행이고, 둘 다 "다음 퍼즐"이라고
  말합니다. 앱이 던지는 가장 무거운 질문인 설정의 전체 삭제 확인이 버튼을
  아직 한 줄로 세우던 유일한 곳이었습니다. 이제 위아래로 쌓이고 파괴적인
  쪽이 위에서 채워진 색을 입습니다. 다른 모든 파괴적인 질문이 이미 그랬듯이.

- **끝낸 퍼즐을 다시 풀어 볼 수 있습니다.** 다시 풀기는 같은 퍼즐을 처음부터
  재생하고 아무것도 기록하지 않습니다. 기록에 남는 시도는 첫 번째에 이미
  정해졌고, 답을 본 뒤에 보낸 정답은 넘겨받은 퍼즐을 깨끗이 푼 것으로 이력에
  남길 테니까요.

- **휴대폰이 방금 무슨 일이 있었는지 말하는 패널에 머무릅니다.** 세 개의
  마무리 화면 모두 퍼즐을 풀거나 라인이 끝나는 순간 엔진 창으로 옮겨
  갔습니다. 평가야말로 돌아와서 보려던 것이라는 전제였지요. 실제로는 방금
  던진 질문에 답하는 패널을 — 퍼즐의 판정과 난이도와 주제를, 라인의 결말과
  그에 대해 할 수 있는 일들의 행을 — 치워 버렸습니다. 엔진은 두 번째
  질문이고, 탭 한 번 거리에 있습니다. 레퍼토리 라인의 끝에서는 엔진이
  켜졌다가 곧바로 다시 꺼지기도 했고, 평가 화면으로 돌아오면 이미 채점한
  자리를 다시 탐색했습니다. 이제 갖고 있던 숫자를 보여 줍니다.

- **보여 주는 해답의 마지막 수도 수입니다.** 두 트레이너는 해답을 한 박자에
  한 수씩 재생하는데, 마지막 수에서 그 자리 분석이 새 보드로 갈아 끼워졌고
  새 보드는 움직이는 대신 그리기만 합니다. 그래서 라인의 모든 수가 제 칸으로
  미끄러졌는데 정작 재생의 목적이던 그 한 수만 그냥 거기 있었습니다. 이제
  갈아 끼우기가 애니메이션 하나를 기다립니다.

- **편집기의 위치 시트에 빠져나올 길이 생겼습니다.** 휴대폰에서 이 시트는
  자기가 편집하는 보드를 덮고, 필드는 곧바로 반영됩니다. "다 했다"는 뜻으로
  누를 것이 없었고, 보드가 보이지 않는 채로 잘못 건드린 것을 되돌릴 방법은
  아예 없었습니다. 이제 취소와 적용으로 끝나고, 바꾼 것을 지키는 것은
  적용뿐입니다. 시트의 꺾쇠도, 바깥의 어두운 면도, 아래로 끌어내리기도,
  버튼을 다시 누르는 것도 조용히 지키고 있었으니, 답이 어느 쪽으로
  나가느냐에 달려 있었던 셈입니다. 둘 차례에는 레퍼토리처럼 킹 글리프가
  붙고, 캐슬링 표시는 이 앱이 동작을 칠하는 색을 벗고 자기가 토글이라고
  소리 내어 말합니다.

- **긴 보관함 경로가 페이지 밖으로 달려 나가는 대신 줄을 바꿉니다.** 윈도의
  경로에는 줄을 바꿀 자리가 역슬래시를 포함해 하나도 없습니다. 320px
  너비에서 실측하니 가장자리 32px 밖에서 끝나고 있었습니다.

## 0.4.4

- **글자가 다른 앱들이 쓰는 크기가 되었습니다.** 본문이 Material과 Fluent와
  GitHub가 하나같이 설명 문구에 남겨두는 12px 칸에 앉아 있었고, 그 아래에는
  이름 없는 11px 단계가 있었습니다. 휴대폰에서 이것은 그 기기의 다른 모든 앱을
  읽는 크기의 70%입니다. 이제 본문은 14px, 제목은 16px, 페이지 머리글은 20px,
  설명 문구는 12px입니다. 그 대가로 목록과 패널에 한 화면당 들어가는 줄이
  줄어듭니다. 치를 만한 값이었습니다.

- **그리고 읽을 수 있는 색이 되었습니다.** 여섯 개의 색이 작은 글씨에 필요한
  4.5:1에 못 미쳤습니다. 처음에 확인했던 두 배경이 아니라 실제로 놓이는 모든
  표면을 상대로 재보니, 조용한 라벨 회색은 어두운 테마에서 3.16:1, 호박색 주의
  표시는 흰빛 페이지에서 2.61:1이었습니다. 이제 모두 통과합니다. 밝은 테마의
  강조색이 눈에 띄게 짙은 파랑이 된 것이 그 대가이며, 주 버튼 위의 흰 글씨는
  미달에서 넉넉함으로 바뀌었습니다.

- **노트의 마지막 줄 아래에 여백이 생겼습니다.** 문서가 페이지가 끝나는 자리에서
  정확히 끝나서, 쓰고 있는 줄이 화면 밑변에 붙어 있었습니다. 이제 그 너머로
  스크롤할 여백이 있고, 그 빈 곳을 누르면 커서가 노트 끝으로 갑니다. 읽기
  모드에서 제목이 아래 선에 눌려 있던 것도 함께 고쳤습니다.

- **오프닝 맵이 방금 추가한 기보를 봅니다.** 기보를 넣고 Grow를 누르면 아무것도
  없다고 했는데, 내 기보를 먼저 열고 같은 Grow를 누르면 되었습니다. 서버는
  처음부터 문제가 아니었습니다. 성공한 *빈* 응답이 페이지가 살아 있는 동안
  캐시되어, 기보가 하나도 없던 시절에 한 번 배운 "기보가 없다"가 계속 답으로
  남아 있었습니다. 이제 빈 응답은 잠정적이며, 기보를 모으면 곧바로 지워집니다.

- **퍼즐 허브가 스스로를 기다리지 않습니다.** 허브가 보내는 다른 요청이 2~5
  밀리초일 때 한 요청만 2.7초가 걸렸고, 페이지는 여섯 개를 모두 기다립니다.
  가장 약한 주제를 세는 질의가 색인이 맞지 않는 테이블을 읽고 있었고, 500개
  묶음마다 2,760만 행을 훑었습니다. 기본 키로 옮기니 2.72초 → 0.004초, 답은
  같습니다. 허브의 버튼 네 개도 페이지가 무슨 말을 할지 정하기 전까지는
  자리표시자로 남습니다.

- **트레이너가 오류 대신 설정 화면으로 보냅니다.** 퍼즐 데이터베이스가 없는
  보관함에서, 트레이너가 설정 화면이 할 조언을 정작 그 일을 할 수 없는 곳에
  적어 두고 있었습니다. 빌드를 마쳤을 때는 같은 버그가 거꾸로 나타나, 데이터
  베이스가 생기기 전의 마지막 실패를 그대로 들고 있었습니다. 다시 만든 데이터
  베이스가 제대로 교체되기도 합니다. 예전 핸들이 교체될 파일을 계속 열고
  있었고, Windows에서는 그 때문에 교체 자체가 실패했습니다.

- **Grow가 무엇을 못 찾았는지, 무엇에서 자랐는지 말합니다.** "색인된 것이 없다"와
  "기준선에 못 미친다"는 서로 다른 답이며 이제 다르게 읽힙니다. 그리고 전체가
  아니라 내 기보의 일부를 가리켜 자라게 할 수 있습니다.

- **라이선스가 앱의 한 페이지가 되었습니다.** 예전에는 링크가 브라우저 탭을
  열었고, 데스크톱 셸에서는 읽고 있던 설정으로 돌아갈 길이 없는 두 번째 창이
  떴습니다. 이제 앱 자신의 머리글 아래 하위 경로이며, 뒤로 가는 갈매기표가
  있습니다.

- **철자가 틀린 아이디는 틀렸다고 말합니다.** 온라인 기보에서 없는 선수를 찾으면
  아무것도 입력하지 않았을 때와 같은 안내가 나왔습니다. 선수 조회의 404는 하나의
  답이며 그대로 전달됩니다. 그 밖의 것은 여전히 네트워크 문제입니다.

- **큰 화면에서 보드가 커집니다.** 어떤 디스플레이에서도 736px에서 멈췄습니다.
  보드가 놓인 행에 상한이 걸려 있어서, 27인치 모니터가 13인치 노트북의 보드를
  그렸습니다. 1920×945에서 측정: 736 → 785. 이제는 임의의 숫자가 아니라 창의
  높이가 한계이므로, 화면이 높을수록 더 커집니다.

- **오션이 기본 보드입니다.** 새 보관함에만 적용됩니다. 선택은 기기별로
  저장되므로 이미 열어 본 사람은 쓰던 것을 그대로 씁니다.

## 0.4.3

- **macOS 앱 아이콘을 macOS가 원하는 크기로 그립니다.** 512×512였는데, 이는
  패키저가 받아 주는 최솟값을 정답으로 오해한 것이었습니다. 그래서 만들어진
  `.icns`에는 가장 큰 크기를 담을 자리가 없었고, 레티나 파인더는 매번 512를
  늘려서 그렸습니다. 썸네일이 깨져 보인 이유는 실제로 깨져 있었기
  때문입니다. 이제 1024로 렌더링합니다. 윈도우는 NSIS가 받아 주는 최대치인
  256을 그대로 씁니다.

## 0.4.2

- **데스크톱 앱의 로컬 보관함이 다시 동작합니다. 사실 데이터베이스 드라이버가
  들어 있지 않았습니다.** 폴더를 고르면 "로컬 서버가 시작되지 않았습니다"라고
  했고, 시작되지 못한 그 서버는 "Cannot find package 'better-sqlite3'"로 죽고
  있었습니다. 의존성은 제대로 빌드되어 복사되었지만 앱으로 들어가는 길에
  버려졌습니다. app-builder-lib의 복사 필터는 `if (relative ===
  "node_modules") return false`로 시작하기 때문에, extraResources 원본
  최상위에 있는 바로 그 이름의 디렉터리는 어떤 filter를 줘도 조용히
  제외됩니다. macOS만이 아니라 모든 플랫폼이 그랬습니다. 이제 afterPack
  훅에서 복사하며, 복사가 실패하면 예외를 던집니다. 다음에 이게 깨지면 앱이
  아니라 빌드가 깨집니다.

- **그리고 실패가 어디를 봐야 하는지 알려 줍니다.** 화면은 "터미널 출력을
  확인하세요"라고 했지만, 패키징된 앱은 서버를 `stdio: 'ignore'`로 띄우며
  무엇이 잘못됐는지에 대한 유일한 기록을 버리고 있었습니다. 이제 두 스트림
  모두 보관함 옆 `server.log`에 남고, 화면은 그 파일 이름을 알려 줍니다.

## 0.4.1

- **macOS 빌드에 임시(ad-hoc) 서명을 합니다.** 서명이 아예 없었고 arm64
  macOS는 서명되지 않은 코드를 실행하지 않기 때문에, 내려받은 dmg가
  "손상되었습니다"라는 말과 함께 거부되었습니다. 멀쩡한 다운로드에 대한
  거짓말이고, 그 말을 들은 사람은 파일을 버리게 됩니다. 임시 서명은 비용도
  Apple 계정도 필요 없습니다. 공증(notarisation)은 여전히 없으므로 첫
  실행은 오른쪽 클릭 → 열기이거나 `xattr -dr com.apple.quarantine` 한
  줄이며, 둘 다 이제 `desktop/README.md`에 적혀 있습니다.

## 0.4.0

- **오프닝 맵을 트리로 읽을 수 있습니다.** 같은 노드, 같은 크기, 같은
  간선을 별자리 대신 순서대로 놓습니다. 깊이는 한 축으로, 형제는 다른
  축으로, 부모는 자기가 거느린 자식들의 한가운데에 섭니다. 데스크톱에서는
  옆으로, 휴대폰에서는 아래로 자라고, 마지막으로 본 배치는 기기가
  기억합니다. 트리는 표류하지 않고 점을 끌 수도 없습니다. 그것은
  별자리의 것이고, 트리에서는 곧은 선이 함께 떠돌아 버립니다.

- **넓어서 읽기 힘든 맵을 위한 가지치기.** 노드의 가위는 그 노드까지
  이어지는 수와 그 뒤에 오는 것만 남기고 도중의 형제 노드를 덜어 냅니다.
  노드를 하나씩 접는 수십 번 대신 한 번입니다. 전체 그래프 복원으로
  되돌아가고, 배치를 바꿔도 풀립니다. 화면에 맞추기는 그려진 것을 화면에
  맞추며, 이제 배치를 바꿀 때도 같은 일이 자동으로 일어납니다. 이전의
  이동과 확대는 예전 위치에 대한 답이었으니까요.

- **홈에서 목적지를 아예 내릴 수 있습니다.** 타일을 끄면 격자 아래 줄로
  내려갈 뿐이어서, 전혀 쓰지 않는 목적지도 버튼 하나를 계속 차지했습니다.
  이제 세 번째 자리 — 페이지 밖 — 가 있고, 사용자 지정 시트에 이름으로
  적혀 한 번에 되돌릴 수 있습니다. 무엇도 갇히지 않습니다. 홈이 무엇을
  보여 주든 사이드바와 더 보기가 모든 섹션에 닿습니다.

- **퍼즐 허브가 연습할 만한 테마를 알려 줍니다.** 책을 가져오지 않은
  보관함에서 비어 있던 자리에, 가장 자주 틀리는 테마가 들어갑니다. 집계된
  시도가 다섯 번은 쌓여야 판단하고, 보관함 자신의 평균보다 나쁠 때만
  말합니다. 그렇지 않으면 추측 대신 아무 말도 하지 않으며, 비율이나 레이팅은
  보여 주지 않습니다.

- **기다림이 다가올 것의 모양과 크기를 그대로 갖습니다.** 앱 곳곳의
  자리표시자가 모양이나 높이가 어긋나 있어서 페이지가 채워지며
  재배치되었습니다. 선반 카드는 실제보다 20 px, 게임 행은 16 px 짧았고,
  설정 페이지는 자기 열을 그렸다가 페이지의 열로 뛰어들었습니다. 이제
  보드와 노트의 자리표시자는 비슷해 보이는 값이 아니라 그 페이지의 레이아웃
  상수로 만들어지고, 각 블록은 자기 답이 도착할 때까지 자기 자리를 지킵니다.
  아직 아무도 답하지 않았는데 "여기엔 아무것도 없다"고 말하는 패널은
  기다리는 패널보다 나쁩니다.

- **목록 두 개가 왕복 두 번을 그만두었습니다.** 엘리트 목록은 데이터베이스를
  먼저 묻고 나서야 게임을 물었고, 온라인 아카이브는 월 목록을 먼저 묻고
  나서야 한 달을 물었습니다. 앞은 클라이언트가, 뒤는 서버가 고칠 몫이었고,
  이제 /months가 가장 최근 달의 게임을 그 달을 알려 주는 목록과 함께
  보냅니다.

- **엔진 탭의 라인이 루트부터 읽힙니다.** 커서를 지나가며 만들었기 때문에,
  변화수로 들어가면 그 변화수가 라인이 되고 메인라인이 옆에 괄호로
  놓였습니다. 트리가 커서를 중심으로 재배열되는 것처럼요. 커서는 어디에
  있는지를 표시할 뿐, 문장이 무엇인지 정하지 않습니다.

- **맵 노드에서 분석하면 맵이 아니라 그 라인이 열립니다.** 해석된 트리
  전체를 커서만 그 노드에 둔 채 보드에 넘겨서, 질문 하나가 맵의 다른 모든
  라인을 데리고 도착했습니다.

- 그 밖에: 최근 아카이브 검색이 사이트별이 아니라 하나의 목록이 되었고,
  아카이브 창이 엄지 아래에서 커지는 대신 끝날 높이로 열리며, 제목을 두 번
  눌러 이미 할 수 있던 "이름 짓기" 버튼이 스터디·노트·책에서 사라졌고,
  세그먼트 컨트롤의 각 칸이 라벨만큼의 너비를 갖고, 시작 위치의 상세가
  휴대폰에서 키보드와 함께 열리지 않습니다.

## 0.3.0

- **레퍼토리의 새 게임 패널이 서식처럼 읽힙니다.** 버튼 네 개 — 모드와
  플레이할 색 — 가 같은 크기와 모양으로 한 덩어리를 이루고 있었고, 어느
  쌍이 무엇을 고르는지 알려 주는 것이 없었습니다. 이제 두 쌍 모두 아래의
  출처·레이팅 필드와 같은 방식으로 라벨을 답니다. 색을 고르는 쪽에는 그
  색의 킹이 붙어, 어느 쪽을 먼저 보든 두 줄을 혼동할 수 없습니다. 자유
  대국 모드의 이름도 *스파링*에서 **자유 대국**으로 바뀌었습니다.

- **퍼즐 탭이 손 닿는 곳으로 갑니다.** 휴대폰에서 퍼즐 탭은 대시보드를
  열었습니다 — 집계와 난이도별 성적, 200줄짜리 기록. 가끔 들여다볼
  페이지이지 훈련하러 갈 때마다 마주칠 페이지는 아닙니다. 게다가 그
  페이지를 허브로 쓰게 해 주던 바로가기 셋은 맨 위에 붙어 있었는데,
  한 손으로 쥔 휴대폰에서 엄지가 닿지 않는 바로 그 구석입니다. 이제
  탭은 런처를 엽니다. 훈련 버튼과 테마·책·대시보드 타일이 모두 탭 바
  바로 위, 화면 아래쪽에 놓입니다. 복습할 퍼즐이 있으면 그 개수와 함께
  거기 나타납니다. 이 페이지는 네트워크를 기다리지 않고도 쓸 수 있으며,
  늦게 도착하는 두 가지는 손을 뻗기 시작한 뒤에 버튼이 움직이지 않도록
  배치했습니다. 여기서 연 곳은 모두 여기로 돌아옵니다. 데스크톱은
  그대로입니다 — 사이드바가 네 곳을 처음부터 담고 있었습니다.

- **오프닝 맵이 한 번에 색을 입습니다.** 맵을 필드와 대조하는 일은 맵에
  그려진 포지션마다 요청 하나씩 — 휴대폰에서 작은 서버까지 수백 번의 왕복 —
  을 치렀고, 참고 데이터베이스는 답을 그때그때 합산했는데, 루트 근처에서는
  그것이 방문할 때마다 데이터베이스 대부분을 더하는 일이었습니다. 이제 모든
  출처가 몇 번의 묶음 요청으로 맵 전체에 답하고(오프닝 이름 라벨도 — 같은
  홍수가 한 요청씩 흐르고 있었습니다), 참고 데이터베이스는 빌드·튠 때 미리
  계산한 수별 합계로 필터 없는 질문에 답하며(실 서버에서 측정: 첫 배치의
  4.7초 합산이 2 ms가 됨), Lichess 출처는 하루 안의 두 번째 방문부터
  프록시의 디스크 캐시로 답합니다. 페이지는 커버리지, 이름, 필드가 모두 답할
  때까지 캔버스를 잠깐의 스피너 뒤에 붙들었다가 완성된 그림을 한 번에 통째로
  보여줍니다 — 기한이 지나면 느린 출처는 맵을 볼모로 잡는 대신 예전의 점진적
  색칠로 물러납니다.
- **맵이 어떤 크기에서도 움직입니다.** 도착 서곡, 한가한 떠돎, 드래그 물리는
  모두 매 프레임 React로 장면 전체를 다시 그리는 방식으로 움직였습니다 — 점
  하나에 SVG 요소 여덟 개꼴, 초당 예순 번 — 그것이 160개 노드를 넘는 맵이
  정지해 있던 이유입니다. 이제 애니메이션 루프 하나가 위치를 SVG에 직접 쓰고
  React는 장면을 한 번만 그리므로 상한이 사라졌습니다: 400개 노드의 맵도
  작은 맵처럼 흩어졌다 제자리를 찾고, 숨 쉬고, 끌면 거미줄처럼 딸려옵니다.
- **오프닝 맵은 자기 페이지가 되었고, 그렇게 읽힙니다.** 도구 아래의 하위
  항목으로, 보드와 편집기 옆에 한 칸 들여쓰여 있었습니다. 그것은 맵의 정체가
  아닙니다. 맵은 레퍼토리를 늘 지켜보는 화면이고, 게임이나 스터디와 같은
  종류의 목적지입니다. 이제 사이드바에 최상위 줄을 갖습니다. 휴대폰은 여전히
  더보기를 거쳐 갑니다. 하단 바에 일곱 번째 탭을 놓으면 탭 하나가 50px
  남짓이 되고, 그 안에 앱에서 가장 긴 라벨을 담아야 하기 때문입니다. 그 김에
  페이지가 앱의 나머지를 따라잡았습니다. 다른 모든 페이지와 같은 크기의 같은
  제목이 가장자리에서 같은 거리에 놓이고, 검색 칸은 자기 줄을 가지며, 맵의
  두 컨트롤은 테두리 상자에 담기는 대신 표면에 바로 그려집니다. 상세 패널은
  항목과 통계 줄이 더는 줄바꿈되지 않을 만큼 넓어졌습니다 — 예전의
  18rem에서는 거의 모든 줄이 깨졌습니다. 캔버스는 헤더 뒤로 흐르지 않고 그
  아래에서 시작하므로, 페이지 제목을 가로질러 지나가는 것이 없습니다.
- **강조색은 지금 무엇을 보고 있는지에 답합니다.** 예전에는 커버된 점이 모두
  앱의 강조색으로 그려져서, 메인라인을 같은 강조색으로 밝히는 것은 수백 개의
  강조색 중 하나를 더하는 일이었습니다. 이제 바탕은 중립색이고 색은 한
  가지에만 쓰입니다. 점을 고르면 거기서부터 필드가 걷는 길이 끝까지 켜지고,
  루트까지의 계보는 밝은 전경색으로 답합니다 — 어디서 왔는지와 대세가 어디로
  가는지를 한눈에. 검색하면 모든 히트가 동시에 그 대접을 받되 선마다 자기
  색을 입으므로, "내가 실제로 마주하는 방어는 무엇이고 어디로 가는가"가 한
  번의 질의가 됩니다. 아무것도 고르지 않으면 맵은 고요합니다. (여기까지 오는
  데 규칙이 두 번 바뀌었습니다. "모든 노드에서 최다 자식을 표시"는
  메인라인을 표시하는 것처럼 들리지만 거의 전부를 표시합니다. 외동 자식은
  자동으로 최다이기 때문입니다 — 시험용 맵에서 63개 간선 중 54개. 그리고
  검색 *히트*마다의 색은 *선*마다의 색이 아닙니다. 변화 여섯 개짜리 루이
  로페즈에서 "Ruy"는 18개 노드에 걸리고 그 대부분은 다른 히트의 선 위에 서
  있습니다. 베를린이 메인라인과 같은 색으로 나왔던 이유입니다.)
- **별자리는 하나의 거미줄처럼 움직입니다.** 점을 끌면 그 점만 움직이고
  나머지는 얼어붙었습니다. 이제는 딸려옵니다. 잡힌 점은 손가락을 따라오고,
  이웃은 저마다의 스프링을 따라오며, 붐비면 서로 밀어냅니다. 손을 떼도 뚝
  멈추는 대신 관성으로 굴러가 멎습니다. 놓아둔 자리가 그대로 그 자리가
  됩니다. 핀치 확대도 어디서든 됩니다. 두 점 위에서 시작해도 됩니다 —
  예전에는 두 손가락이 모두 빈 바탕에 닿아야 했는데 촘촘한 맵에는 빈 바탕이
  거의 없어서, 휴대폰에서 확대가 사실상 불가능했습니다. 고른 점을 한 번 더
  누르면 선택이 풀리고, 떠 있는 버튼은 더 이상 돌아다니지 않습니다. 구석에
  고정되어 있고, 휴대폰 전용입니다. 마우스는 페이지의 컨트롤을 페이지 자신의
  구석에서 찾기 때문입니다.

- **저장은 직접 하는 것이 되었고, 읽는 중에도 기물이 움직입니다.** 게임,
  스터디, 노트는 1.5초마다 스스로 저장했고, 편집 모드는 읽는 사람이 읽고
  있는 것을 덮어쓰지 않게 막으려고 있었습니다 — 연필을 누르기 전까지 보드가
  수를 거부했던 이유가 그것입니다. 이제는 요청 없이는 아무것도 쓰지 않으므로
  그 방어막은 지킬 것이 없습니다. 보드는 두 모드 모두에서 살아 있고, 연필은
  겉보기 그대로 NAG 팔레트와 주석 상자와 수순 편집 도구를 켜고 끄는 전환이
  되었습니다. 어느 쪽에서 만든 변경이든 하나의 대기 중인 변경이며, 저장 안
  됨 표시와 그 옆의 저장· 버리기 버튼, `Ctrl/⌘ S`, 그리고 나갈 때의 저장 /
  버리기 / 여기 머무르기 질문이 따라붙습니다 — 브라우저의 뒤로 가기 버튼에도
  똑같이 적용됩니다. 자동 저장은 설정 › 문서에 있는 항목이며 기본값은
  꺼짐입니다. 켜면 예전 동작 그대로입니다. 조용히 버려지던 세 가지 — 엔진
  수순 따라가기, 탐색기 수 두기, 변화수를 메인 라인으로 올리기 — 도 이제는
  보관할 수 있는 평범한 대기 중 변경입니다. 보드를 돌리는 것은 여전히
  편집으로 치지 않습니다.
- **문서는 저장하지 않은 사본을 보관함에 남깁니다.** 직접 저장에는 구멍이
  하나 있습니다. 대기 중인 내용은 탭 안에만 있으므로 브라우저가 죽으면 함께
  사라집니다. 이제 마지막 편집 몇 초 뒤에 사본이 문서 옆에 놓이고, 열 때
  그것이 발견되면 되돌려 줄지 묻습니다 — 복구하거나, 버리거나. 복구한 내용은
  저장하지 않은 상태로 돌아오므로 보관하기 전에 확인할 수 있고, 몰래
  적용되는 일은 없습니다. 사본이 브라우저가 아니라 보관함에 있으므로, 주석을
  달다 죽은 휴대폰의 작업을 책상에서 되살릴 수 있습니다. 이 사본은 이름을
  바꾸면 따라가고, 문서를 지우면 함께 사라지며, 저장하거나 버리는 순간
  없어지고, 보관함의 기록 저장소에는 들어가지 않습니다.
- **시작 화면은 OS가 그리는 시작 이미지, 그것뿐입니다.** 설치된 휴대폰
  앱은 OS 자신이 그리는 나이트로 열립니다 — 시작 이미지 72장(기기 18종 ×
  가로세로 × 밝기 두 가지)과 그 `<link>` 태그는 `scripts/render-icons.mjs`가
  함께 만듭니다 — 그리고 청크가 오는 동안은 조용한 앱 색깔의 바탕입니다.
  그 위에 페이지 안 시작 화면을 만들었다가 걷어냈습니다. OS의 스플래시 뒤에
  또 하나의 스플래시는 또 한 번의 넘겨주기이고, 넘겨주기마다 이음매가
  남습니다(`web/index.html`이 그 사연을 담고 있습니다). 이미지는 앱에 저장된
  설정이 아니라 **휴대폰의** 밝기 설정을 따릅니다 — iOS가 이미지를 고를 때
  알 수 있는 것이 그것뿐이기 때문입니다. 안드로이드는 클라이언트 힌트로 지금
  휴대폰이 있는 밝기의 `background_color`를 받습니다. 브라우저 탭에는 이 중
  아무것도 나오지 않습니다. 어차피 뜰 페이지 앞을 브랜드 카드로 가리는 것은
  그저 스플래시 화면을 위한 스플래시 화면이기 때문입니다.
- **북마크, 그리고 그것에 합의한 세 선반.** 핀이 북마크가 되면서 게임·
  스터디·노트가 같은 아이콘과 같은 순서를 씁니다. 스터디와 노트에도 게임에만
  있던 빈 상태 두 가지 — 북마크한 것이 없을 때와 검색에 걸리는 것이 없을 때
  — 가 생겨서, 필터가 전부를 감췄을 때 텅 빈 보관함처럼 보이는 대신 그렇다고
  말합니다. 검색 필드와 북마크 스위치는 이제 자기가 걸러 내는 대상 옆에
  있습니다. 넓은 화면에서는 모음 패널의 헤더 안에, 휴대폰에서는 자기만의
  줄에 놓입니다.
- **넘겨 본 게임이 캐시되고, 그 캐시를 비울 수 있습니다.** 여러 사람의
  기보를 넘겨 보면 그것이 조용히 디스크를 채웠고, 정작 가장 자주 보는 *이번
  달*에는 캐시가 아무 일도 하지 않았습니다. 이제 chess.com의 달은
  `If-Modified-Since`로 다시 확인해 바뀐 것이 없으면 304 하나로 끝나고,
  Lichess는 마지막으로 본 뒤의 새 게임만 받아 합칩니다. 설정에서 무엇이
  얼마나 저장돼 있는지 보이고, 전부 지우는 버튼이 하나 있습니다.
- **회색조 배색과, 읽히는 오프닝 이름.** Mono는 표면에서만 색을 뺐기 때문에
  색 없는 앱에 파란 버튼과 링크와 내비게이션 강조가 남아 있었습니다. 이제
  강조색에도 따로 색조 손잡이가 있어서 회색조가 정말 회색조입니다. 그와
  별개로 ECO 태그와 오프닝 이름은 밝은 테마의 페이지 위에서 2.3:1이었습니다
  — 측정한 값이고, 지금은 7.0~7.2:1이며 어두운 테마는 그대로입니다.
- **책 제목을 한국어로 지을 수 있습니다.** '제목 없는 책'로 책을 만들면 "그
  제목은 폴더 이름이 될 수 없습니다"로 실패했습니다. 슬러그가 ASCII 허용
  목록이라 한글을 전부 걷어낸 뒤 남은 빈 문자열을 거절한 것입니다. 파일
  시스템이 담을 수 있는 제목이면 보관함도 담습니다.
- **PGN에서 가져온 스터디가 스스로 이름을 답니다.** Lichess는 스터디 이름을
  `[StudyName]`에, 챕터 이름을 `[Event]`에 씁니다. 모든 게임의 값이 일치할
  때 제목 칸이 앞의 것으로 채워지고, 일치하지 않으면 손대지 않습니다.
- **페이지 제목에서 아이콘이 빠지고**, 버튼 줄이 없는 창에 나가는 길이
  생겼습니다. 옆의 단어를 그대로 되풀이하는 작고 흐린 글리프가 제목 다섯
  군데에 있었고, 전부 `aria-hidden`이었습니다. 건드리는 대로 적용돼서 취소가
  없는 엔진 설정과 퍼즐 난이도 선택 창에는 데스크톱에서만 X가 붙습니다 —
  마우스에는 밀어서 닫는 동작이 없기 때문입니다.
- **고침: 눌렀다 뗐는데 멈추지 않던 이동 버튼.** 보드에서 앞으로 버튼을
  누르고 있으면 페이지를 떠날 때까지 게임이 저 혼자 넘어갈 수 있었습니다.
  반복 타이머가 다시 그릴 때마다 새로 만들어졌고 — 엔진이 켜져 있으면 초당
  여러 번입니다 — 손을 뗄 때 취소된 것은 실제로 돌고 있던 타이머가
  아니었습니다. iPad에서 발견했지만 iPad만의 문제였던 적은 없습니다.
- **내 게임을 탐색기에서 봅니다. 더 이상 북이 아닙니다.** 0.2.4에서는
  보관함의 게임으로 오프닝 북을 만들 수 있었습니다. 동작하지 않았습니다.
  북은 소스를 `basename()`으로 되돌려 주는데, 그 과정에서 "이건 보관함
  게임"이라는 접두사가 사라져 다시 만들기가 400을 돌려줬습니다. 애초에
  동작할 수도 없었습니다 — 북의 소스 목록은 고정이라, 다시 만들어도 같은
  게임을 다시 읽을 뿐 그 뒤에 둔 게임은 결코 들어오지 않습니다. 그리고 형태
  자체가 틀렸습니다. 북은 만들 때 결과를 미리 합산하므로 오직 한 가지
  질문에만 답할 수 있습니다. 이제 탐색기에 **내 게임** 소스가 있습니다.
  보관함을 직접 색인하고 질의할 때 거릅니다 — 내가 잡은 색, 이겼는지
  여부*(결과 문자열이 아니라 내 색을 기준으로 읽습니다)*, 시간제, 날짜. 만들
  것도 다시 만들 것도 없습니다. 게임을 담는 순간 반영되고, 목록의 게임은
  보드에서 열립니다.
- **Pretendard가 앱의 서체가 되었습니다.** Inter에는 한글이 없어서 모든
  한국어 문장이 OS가 가진 아무 글꼴로 떨어졌고, 한 화면에 두 서체가 두 가지
  크기로 섞였습니다. 기기마다 다르게요. 이제 영어 UI는 37 KB짜리 서브셋
  하나만 받고(대체된 Inter보다 작습니다), 한국어는 실제로 그리는 블록만
  받습니다. 수는 더 이상 고정폭이 아닙니다. SAN은 열이 아니라 단어입니다.
- **나머지 UI도 한국어로 말합니다.** 내비게이션, 퍼즐 안내문, 필터 칩,
  보드의 선수 이름 자리, 편집기의 둘 차례 버튼, 그리고 73개 퍼즐 테마 전부.

## 0.2.4

- **소리가 우리 것이 되었습니다.** 수와 기물 잡기 소리는 lichess의
  `standard` 세트를 바이트 그대로 쓴 것이었고, 그 세트는 lila의 비자유
  목록에 있습니다 — 모든 설치 파일에 담기고 데모가 서빙하면서 그럴 권리는
  없었습니다. 합성한 19개 테이크로 교체했고, 설정에 소리 카드가 생겼습니다.
  어떤 테이크로 할지, 음량, 또는 번갈아 재생해서 긴 분석에서도 같은 샘플이
  반복되지 않게 할 수 있습니다.
- **라이선스가 빌드와 함께 갑니다.** `licenses/`는 저장소를 열지 않는
  사람에게는 닿지 않았습니다. 이제 모든 빌드가 그 텍스트를 싣고, 이 앱을
  이루는 931개 항목을 모두 담은 페이지를 함께 냅니다 — 동봉 자산, npm
  패키지, Electron의 Chromium 구성요소 773개까지(0.3.0 무렵의 빌드에서는
  937개 — 페이지가 스스로 셉니다) — 각 항목을 펼치면 해당
  라이선스 전문이 나오고 소스 링크가 붙습니다. 빌드할 때 `node_modules`에서
  만들어지므로 낡을 수가 없습니다.
- **내 기보로 만드는 오프닝 북.** 북은 "여기서 무엇을 두는가"에 답합니다. 내
  기보를 넣으면 "나는 여기서 무엇을 두고, 결과는 어땠는가"에 답합니다 — 같은
  승/무/패 데이터를 나 자신에게 비춰 읽는 것입니다. 도구 아래 새 페이지에서
  보관함의 기보나 올린 모음으로 북을 만듭니다.
- **기보를 한 번에 여러 개.** 기보 목록에서 여러 개를 골라 한 번에
  추가하거나, 달 선택기에서 *전체 기간*을 골라 전체 이력을 가져올 수
  있습니다. PGN 모음 올리기도 앱 안에서 됩니다 — 예전에는 `vault/sources/`에
  파일을 직접 복사하라고 했는데, 휴대폰으로는 할 수 없는 일이었습니다.
- **한국어, 빠짐없이.** 상태와 오류 메시지는 언어와 상관없이 영어였고,
  서버가 보내는 것까지 그랬습니다. 이제 모두 번역되어 있으며, 항목이 없는
  메시지는 깨지지 않고 영어로 표시됩니다.
- **macOS·리눅스 데스크톱 빌드가 실제로 됩니다.** 그동안 안 됐습니다. 잘못된
  `linux.desktop` 스키마가 모든 플랫폼을 실패시켰고, macOS 아이콘은 최소
  512px에 못 미쳤으며, `.deb`에는 관리자 정보가 없었습니다. 워크플로를 읽는
  대신 돌려 보고 찾았습니다.
- **모든 의존성을 갱신**했습니다. TypeScript 7과 Vitest 4를 포함하며, CI
  액션도 지원이 끝난 Node 20에서 벗어났습니다.

## 0.2.3

- **자동 업데이트가 이제 진짜로 동작합니다.** 여태 한 번도 동작한 적이
  없었습니다. electron-updater는 CommonJS라서 `await import()`가 내보낸
  것들을 `.default` 아래에 두고, `{ autoUpdater }`로 구조 분해하면
  undefined가 나왔습니다. 그래서 확인은 실행할 때마다 예외를 던졌고, 그
  오류는 아무도 열지 않는 콘솔로 갔으며, 피드가 올바른 빌드 셋을 서빙하는
  동안 앱은 0.1.0에 앉아 있었습니다. 손으로 설치해야 하는 마지막
  릴리스입니다.
- **데스크톱 앱의 서버가 더 이상 네트워크에 응답하지 않습니다.** 로컬 모드에
  비밀번호가 없는 것은 의도였지만, 모든 인터페이스에 묶여 있었습니다 —
  그래서 공용 네트워크에서 앱을 열면 인증 없는 보관함이 그 네트워크에
  공개되었습니다. 이제 루프백 전용입니다.

## 0.2.2

- **설정이 무엇이 돌고 있는지 말해 줍니다** — 서버의 버전과, 데스크톱
  앱에서는 앱 자신의 버전을, 무엇을 찾았는지 알려 주는 확인과 함께 보여
  줍니다. 헬스 엔드포인트가 두 릴리스 동안 0.1.0을 하드코딩해 돌려주는 사이
  다른 엔드포인트는 진짜 값을 읽고 있었고, 그래서 같은 페이지가 자기 자신과
  어긋날 수 있었습니다.
- **캐슬링을 양쪽 방식으로** — 킹을 g1에 놓든 자기 룩 위에 놓든, 손에 익은
  대로 하면 됩니다. 둘 다 언제나 합법이었고, 제공되던 것만 하나였습니다.
- **macOS** — 셸이 창을 모두 닫아도 계속 살아 있다가 독에서 돌아오고,
  `npm run desktop:package:mac`이 dmg를 만듭니다. 서명이 없으므로
  Gatekeeper가 처음 한 번은 오른쪽 클릭 후 열기를 요구합니다.

## 0.2.1

- **설정에서 보관함 전환** — 데스크톱 셸이 가리키는 곳을 바꾸는 유일한
  방법이 Alt 뒤에 숨은 메뉴 바였습니다. 이제 설정 안의 카드이며, 이야기할
  셸이 있을 때만 보입니다.
- **다시 배포해도 열려 있던 창이 검게 죽지 않습니다** — 모든 섹션이 해시가
  붙은 청크라서, 배포는 이미 열려 있는 페이지가 곧 요청할 청크를
  갈아치웁니다. 이제 그 페이지는 검은 화면으로 무너지는 대신 스스로 한 번
  새로고침합니다.
- **색 구성표** 프리셋 여섯 개. 색 견본 옆의 빈 곳을 눌러도 더 이상 조용히
  첫 번째가 선택되지 않습니다 — 견본들이 label 안에 있었고, label은 엉뚱한
  클릭을 자기가 처음 찾은 컨트롤로 넘깁니다.
- 사이드바가 언제나 로컬이라고 주장하는 대신 접속한 호스트의 이름을
  말합니다.

## 0.2.0

공개 저장소가 아니라 자신의 서버에서 제공된 첫 데스크톱 릴리스이자, 책
가져오기가 설정을 전혀 필요로 하지 않게 된 첫 릴리스입니다.

- **노트에 서식 도구 모음이 생겼습니다** — 굵게부터 인용까지, 그리고 보드
  삽입까지. 노트의 머리글과 함께 페이지 맨 위에 고정됩니다. 다른 두 위치를
  시도했다가 물렸습니다. 선택 영역 위에 떠 있게 하면 무언가를 넣기 전에
  글자를 먼저 선택해야 했고, 키보드 위의 바는 iOS 자체의 보조 줄과
  부딪혔습니다.
- **게임·스터디·노트에서 편집이 하나의 개념입니다** — 머리글의 편집 버튼
  하나, 제목을 더블클릭해 이름 바꾸기, 패널에 숨겨진 것 없음.
- **책 가져오기가 책을 스스로 알아냅니다** — 번호 매김, 라벨 위치, 표기법,
  정답 페이지, 피겨린 기호까지 전부 도출합니다. 1001 Chess Exercises에서
  재생으로 검증된 정답 714개를 얻었고, 손으로 조율한 오프라인 파이프라인이
  글리프 패스 없이 낸 707개를 앞섰습니다.
- **책을 여는 무게가 몇 분의 일이 되었습니다** — 가장 큰 책의 목록이 1,856
  KB에서 283 KB가 되었고, 격자는 화면에 보이는 줄만 만들며, 책장의 표지는
  1.77 MB에서 262 KB가 되었습니다.
- **모든 페이지가 불러오는 것의 모양을 보여 주고**, 기다림이 짧으면 아무
  말도 하지 않습니다.
- **설치 파일이 376 MB가 아니라 108 MB입니다** — 서버의 의존성 트리 전체를
  싣고 있었습니다.

## 2026-08-10

- **책을 가져오는 데 설정이 필요 없습니다** — 앱이 책의 번호 매김,
  다이어그램 옆 번호의 위치, 정답을 적는 방식과 그 위치, 피겨린 기호의 뜻을
  전부 책에서 알아냅니다. 1001 Chess Exercises에서는 재생으로 검증된 정답
  714개이며, 손으로 조율한 오프라인 파이프라인이 자체 글리프 패스 없이 낸
  707개를 앞섭니다. 스캔이 통째로 잃어버린 번호는 그것이 남긴 빈틈에서
  추론합니다. 보드 복구는 비용을 밝힌 체크박스로 제공하는데, 20분에 퍼즐
  26개를 사기 때문입니다.
- **책을 여는 것이 훨씬 가벼워졌습니다** — 책의 목록이 이제 풀이 화면과 원본
  창만 읽는 포지션, 정답, 근거, 시각 정보를 싣지 않습니다. 가장 큰 책이
  1,856 KB에서 283 KB가 되었고, 격자는 타일 5천 개 대신 화면에 보이는 줄을
  만듭니다. 표지는 썸네일 다섯 개를 위한 페이지 렌더 1.77 MB였던 것이 262
  KB가 되었습니다.
- **불러오는 것이 불러오는 것처럼 보입니다** — 모든 목록과 보드와 폼이
  기다리는 동안 자기 모양을 스케치하고, 빠른 로딩에서는 아무것도 보이지 않을
  만큼 늦게 나타납니다. 수순 패널은 자기가 담은 변화의 이름을 답니다. 보드와
  게임에서는 오프닝(ECO 코드와 함께), 스터디에서는 챕터입니다.
- **편집이 하나의 개념입니다** — 게임과 스터디가 노트를 따릅니다. 머리글의
  편집 버튼 하나, 제목 더블클릭으로 이름 바꾸기, 패널에 숨은 두 번째 연필
  없음.
- **공개 저장소 없는 데스크톱 릴리스** — 셸이 서버가 호스팅하는 피드에서
  갱신되므로 소스는 비공개로 남을 수 있습니다. 설치 파일도 서버의 의존성
  트리를 싣기를 그만두었습니다: 376 MB에서 108 MB로.

## 2026-08-09

- **어디서나 읽기와 편집의 구분** — 스터디, 게임, 노트의 보드가 기물이 잠긴
  읽기 모드로 열리고, 연필(또는 노트의 편집)이 수 두기와 NAG와 주석을 풀어
  줍니다. 게임에도 같은 전환이 생겨서 놓친 곁가지를 의도적으로 기록할 수
  있습니다.
- **책 트레이너를 다시 짰습니다** — 책 풀이 화면이 이제 Lichess 트레이너의
  배치를 따릅니다. 퍼즐 패널이 상태와 동작(제출 포함)을 소유하고, 퍼즐
  격자는 패널 머리글에서 펼쳐지며, 휴대폰의 아래 띠가 수를 이동합니다.
  중복되던 모바일 동작 바는 사라졌습니다.
- **레퍼토리가 자랐습니다** — 3,810개 ECO 오프닝 전부를 오프닝 선택기에서
  검색할 수 있고(추린 목록은 기본값으로 남습니다), 첫 수가 수순 패널에
  보이며, 시작할 때 세팅된 오프닝의 마지막 수가 강조되고, 응수가
  즉시-아니면-느리게가 아니라 일정한 속도로 옵니다.
- **정답 창을 되살리고, 지켰습니다** — 다시 가져오기가 세 책 모두에서 정답
  챕터 정보를 조용히 떨어뜨렸습니다. 페이지를 다시 렌더링해 다시 새겼고,
  이제 가져오기가 다시 만들 때도 그 정보를 함께 가지고 넘어갑니다.
- **게임 페이지** — 모음 상자가 항상 자리에 있고(빈 상태도 그 안에 삽니다),
  뒤로 가기를 해도 기보 탐색이 살아남으며, 게임 미리 보기가 뒤의 게임으로
  클릭을 흘려보내지 않습니다.
- **노트** — 책장이 마지막으로 만진 순서로 뒤섞이지 않고 안정된 이름 순서를
  유지합니다.
- **초안이 퍼즐 목록에 합류했습니다** — 가져왔지만 풀리지 않은 다이어그램이
  별도 패널이 아니라 책의 퍼즐 목록에 초안 등급으로 나타납니다.
- **예시 콘텐츠** — 주석 달린 Morphy와 Anderssen의 게임, 스터디 둘, 서로
  연결된 노트 둘이 첫 실행 예시로 함께 옵니다. README 스크린샷이 그것을 보여
  줍니다.
- **운영** — 배포가 웹 앱을 로컬에서 빌드해 `dist/`를 보내고(2 GB 서버가
  빌드에서 메모리 부족으로 죽던 일이 끝났습니다), SSH는 공개 22/80/443을
  닫은 채 테일넷을 타며, 데스크톱 앱은 GitHub 릴리스에서 자동으로
  갱신됩니다.

## 2026-08-08

- **책 두 권을 더 읽어 냈습니다** — 책별 가져오기 설정, 영속 탐색 캐시를
  갖춘 엔진 풀, 그리고 정답으로 제약한 보드 복구(책 자신의 변화가 잘못 읽은
  다이어그램을 고칩니다). 성능 측정용 스캔에서 책 세 권에 걸쳐 검증된 퍼즐이
  약 3,150개에 이르렀습니다. 책에서 나온 것은 가져온 사람의 보관함에 남을
  뿐, 이 저장소에는 들어오지 않습니다.
- **신뢰 등급** — 가져온 모든 퍼즐이 정직한 출처 표시를 답니다(book solution
  → 엔진 검증 → 미검증). 평범한 말로 된 라벨과 모양으로 구분되는 아이콘을
  씁니다.
- **CellNet 미세 조정** — 보드 판독기가 책 자신의 검증된 포지션으로 다시
  학습합니다. 2회차에서 보드 1,891개에 도달했습니다.
- **iOS 키보드 교훈** — 앱이 키보드 애니메이션과 맞서기를 그만두고, 키보드가
  닫힌 뒤에만 스크롤을 바로잡습니다.

## 2026-08-07

- **책 퍼즐이 도착했습니다** — ML 가져오기 파이프라인이 스캔한 전술책을
  읽습니다. 다이어그램 검출, CellNet 포지션 판독, 인쇄된 정답 해석, 재생
  검증, 그리고 원본 스캔을 한 번에 들춰 볼 수 있는 퍼즐별 근거 크롭까지.
- **퍼즐 트레이너** — 난이도 구간이 있는 Lichess 테마 훈련, 진행 대시보드,
  실패한 퍼즐 복습.
- **모바일 정비** — 수 이동 역할을 하는 상황별 하단 바, 보드 아래 한 창
  레이아웃, 터치에 맞춘 미리 보기와 선택기.

## 2026-08-06

- **토대** — 평범한 파일로 된 보관함(Obsidian 내력), Stockfish 18 WASM을
  쓰는 분석 보드, 포지션 편집기, 무손실 왕복을 보장하는 PGN 챕터 스터디,
  보드와 위키 링크를 끼워 넣는 마크다운 노트, 손수 고른 게임 모음,
  chess.com·Lichess 기보 탐색, 엘리트 참고 게임, 오프닝 탐색기, 정확도와
  브릴리언트 판정이 있는 게임 리뷰, PWA와 Electron 데스크톱 셸, 선택적
  TOTP가 있는 비밀번호 게이트.
