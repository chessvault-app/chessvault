[package]
name = "chessvault-core"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Native pipeline for Chess Vault's reference-games heavy jobs"

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
shakmaty = "0.30.1"

[profile.release]
lto = true
